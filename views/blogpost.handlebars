<section class="container">
  <section class="flex-row justify-center">
    {{#each board.posts as |post|}}
    <section class="card">
      <a href="/board/{{id}}">
        <header>{{title}}</header>

        <div class="card-body">
          {{!-- <img src="/images/{{filename}}" alt="{{description}}"> --}}
          {{!-- <p>By: {{artist}}</p> --}}
        </div>
      </a>
    </section>
    {{/each}}
  </section>
</section>